= render "teacher/subjects/subjects_dropdown", subjects: @school_class.subjects_active, school_class: @school_class

= page_header "Oceny klasy #{@school_class.name}", @subject.name

= render "teacher/subjects/marks_table", descriptions: @descriptions, subject: @subject, students: @students

:javascript
  $(document).ready(function() {
    $(".description").popover();
  });
